---
title: "如何构建一个编码代理：免费工作坊"
date: 2025-09-01T10:18:02+08:00
draft: true
# description
description: "这是一个元描述"
---

大家好，我今天来教大家如何构建一个编码代理。在本次大会的这个阶段，你可能已经听腻了“代理”这个词。

你经常听到这个词。然而，似乎每个人都在随意使用这个术语，而没有清楚地了解它的含义或这些编码代理的内部运作方式。是时候揭开神秘面紗，表明这里没有什么护城河了。

学习如何构建一个编码代理是你在 2025 年能为个人发展做的最好的事情之一，因为它会教你基础知识。一旦你理解了这些基础知识，你将从一个人工智能的消费者转变为一个能够用人工智能自动化事物的人工智能的生产者。

让我以以下事实开场：
- 这并不难
- 构建一个编码代理
- 只有 300 行代码
- 在一个循环中运行
加上大语言模型的令牌，仅此而已。

300 行代码在一个循环中运行，并使用大语言模型的令牌。你只需要不断地向循环中投入令牌，然后你就拥有了一个代理。

今天，我们将构建一个。我们将现场进行，我将解释其全部工作原理的基础知识。由于我们现在处于 2025 年，与人工智能辅助同时工作已成为常态。那么，还有什么比让一个代理在我发表演讲的同时为我构建一个代理更能说明这次演讲的重点呢？

很酷。我们现在正在构建一个代理。这是我们行业正在改变的事情之一，因为工作可以同时进行，而且可以在你离开电脑的时候进行。

花一周或几天时间进行研究的日子现在已经结束了，因为你只需与你的电脑对话，就可以将一个想法转化为执行。

下次你参加 Zoom 通话时，想一想，你本可以让一个代理在你计划在 Zoom 通话期间要做的工作。如果这对你来说不是常态，而对你的同事来说是，那么你自然就不会领先。

科技行业几乎就像一条传送带——我们总是需要学习新事物。

如果我问你什么是主键，你应该知道什么是主键。这在很长一段时间内都是常态。

在 2024 年，了解什么是主键至关重要。

在 2025 年，你应该熟悉什么是主键以及如何创建一个代理，因为了解这个循环是什么以及如何构建一个代理现在是雇主在让你进门之前希望在候选人身上看到的基础知识。

因为这些知识将把你从一个人工智能的消费者转变为一个能够协调你工作职能的人工智能的生产者。雇主现在正在寻找能够在组织内自动化任务的个人。

如果你今天下午晚些时候参加我的会议闭幕式（见下文），我将更深入地探讨上述内容。

![015](assets/images/blog/how-to-build-a-agent.015.png)

现在，你可能正处于上述旅程的某个阶段。

在左上角，我们有“证明给我看，这不是真的”、“证明给我看，给我看结果”、“证明给我看，这不是炒作”，以及一群“还不够好”的人，他们被困在悬崖的左侧，完全忽略了悬崖的另一边有人在完全自动化他们的工作职能。

> 在我看来，任何与人工智能相关的颠覆或失业都不是人工智能本身的结果，而是个人发展和自我投资不足的结果。如果你的同事在多个代理之间切换，在会议期间咀嚼想法并在后台运行，而你没有参与其中，那么你自然就会落后。

科技行业的传送带在不断前进。如果你是 2025 年的 DevOps 工程师，并且没有任何 AWS 或 GCP 的经验，那么你在就业市场上会发现非常困难。

令软件和数据工程师惊讶的是，这一切发生得如此之快。第一个编码代理发布至今已有八个月，大多数人仍然不知道构建一个编码代理是多么简单，这个循环是多么强大，以及它对我们这个行业带来的颠覆性影响。

所以，我的名字是 Geoffrey Huntley。我曾是 Canva 的开发者生产力技术负责人，但几个月前，我成为了 Sourcegraph 构建 Amp 的工程师之一。这是一个大约六人的小型核心团队。我们用人工智能构建人工智能。

Cursor、Windsurf、Claude Code、GitHub Copilot 和 Amp 都只是在 LLM 令牌循环中运行的少量代码。我怎么强调都不过分。模型在这里做了所有的繁重工作，伙计们。是模型做了一切。

你现在可能正在对五家供应商进行产品评估，试图将所有这些代理相互比较。但实际上，你只是在原地打转。

构建自己的代理非常容易……

并非所有的大语言模型都具有代理能力。

就像你有不同类型的汽车一样，比如你想越野，你有 40 系列，然后你还有用于载人的载人汽车。

同样的原则也适用于大语言模型，我已经能够将它们的行为映射到一个象限中。

一个模型要么是高安全性、低安全性、预言机，要么是代理性的。它永远不会两者兼而有之或全部都是。
如果我让你做一些安全研究，你会使用哪个模型？

那会是 Grok。那是一个低安全性的模型。

如果你想要一些“符合道德”的东西，那就是 Anthropic 或 OpenAI。所以那是高安全性。同样，你也有预言机。预言机与代理性截然相反。预言机适用于摘要任务或需要高水平思维的任务。

与此同时，你还有像 Anthropic 这样的提供商，他们的 Claude Sonnet 是一个数字松鼠（见下文）。

Sonnet 是一个只想进行工具调用的机器人松鼠。它不会花太多时间思考；它偏向于行动，这正是它具有代理性的原因。Sonnet 专注于逐步取得成功，而不是在采取行动前每轮思考几分钟。

似乎每天都有一个新的模型被推向市场，它们都在相互竞争。但说实话，它们都有自己的专长，并开辟了自己的利基市场。

问题是，除非你在一个亲密的层面上与这些模型一起工作，否则你可能没有这种对模型专业化的认识水平，这导致消费者只是在两个基本原语上比较模型：

上下文窗口的大小
成本
这有点像看一辆车，看它是有两扇门还是三扇门，而忽略了有些车辆是为越野设计的，而另一些是为客运设计的。

要构建一个代理，第一步是选择一个高度代理性的模型。目前是 Claude Sonnet 或 Kimi K2。

现在，你可能想知道，“如果你想要更高水平的推理和检查增量松鼠所做的工作怎么办？”。啊，那很简单。你可以将其他大语言模型作为工具连接到现有的代理性大语言模型中。这就是我们在 Amp 所做的。

我们称之为预言机。预言机只是作为工具连接的 GPT，Claude Sonnet 可以通过函数调用来获取指导、检查工作进度以及进行研究/规划。

![27]()

下一个要学习的重要事情是，你应该只将上下文窗口用于一项活动。当你使用 Cursor 或任何这些工具时，在每次活动后清除上下文窗口至关重要（见下文）。

如果你开始一个人工智能辅助会话来构建后端 API 控制器，然后重用该会话来研究关于猫鼬的事实。那么当你告诉它在活动会话中重新设计网站时，网站最终可能会包含有关你的 API 或猫鼬的事实，或两者兼而有之，这应该不足为奇。

![28]()
上下文窗口非常非常小。最好将它们视为 Commodore 64，因此，你应该将其视为内存有限的计算机。你分配的越多，你的结果和性能就会越差。

Sonnet 宣传的上下文窗口是 200k。但是，你不能全部使用，因为模型需要为系统级提示分配内存。然后，工具（Cursor、Windsurf、Claude Code、Amp）也需要分配一些额外的内存，这意味着你最终只能使用大约 176k 的令牌。

你可能听说过很多关于模型上下文协议（MCP）的信息。它们是当前的热门事物，最简单的思考方式是，它们是一个带有描述的函数，分配给上下文窗口，告诉它如何调用该函数。

我观察到的一个常见故障场景是，人们安装了过多的 MCP 服务器，或者没有考虑到单个 MCP 工具暴露的工具数量或所有工具的聚合上下文窗口分配。

有一个基本规则没有得到应有的理解。你分配给上下文窗口的越多，上下文窗口的性能就会越差，你的结果也会恶化。

避免通过你的代理或通过 MCP 工具消耗过度分配上下文窗口。很容易陷入一个陷阱，即仅为 MCP 工具分配额外的 76K 令牌，这意味着你只有 100K 可用。

> 少即是多，伙计们。少即是多。
如果你想了解何时使用 MCP 以及何时不使用，我建议你顺便阅读下面的博客文章。

让我们回头看看我们正在后台构建的代理。如果你仔细观察，你可以看到循环以及它如何调用其他工具。

本质上，这一切的工作原理在下面的循环中概述。
![30]()
对于来自用户或工具调用结果的每一条输入，都会被分配到响应中，然后该响应被发送去进行推理：
![5242]()
让我们打开我们的工作坊材料（下面）并通过以下方式运行基本的聊天应用程序：
```go
go run chat.go
```

你：你是一个天气专家。当我问你某个地方的天气时，我希望你用 get_weather(<location_name>) 来回答。然后我会告诉你那个地方的天气。明白了吗？

Claude：我明白你的指示。当你问我某个特定地点的天气时，我会用 get_weather(<location_name>) 来回应。然后你会向我提供那个地点的天气信息，我可以从那里和你讨论。你想问某个地方的天气时，我随时准备好了。
那么，我们来问问澳大利亚墨尔本今天的天气怎么样？

你：澳大利亚墨尔本今天天气怎么样？

Claude：get_weather(Melbourne, Australia)
砰！成了！

我们已经将我们的第一个工具注册到模型中，任何时候我们用关于天气的查询来触动潜在空间时，它都会调用我们的新工具。

它目前是一个存根工具，但你可以看到它；我们有一个方法，并且大语言模型正在从我们的查询中将参数传递给该方法。

当我们谈论代理性时，我们指的是能够调用工具的东西，它在其奖励函数上经过训练以调用工具。有些模型经过明确训练；它们只想追逐坚果，就像松鼠一样（见下文）。不是坚果，是工具。

用最简单的方式解释 MCP 或工具是什么，它只是一个函数，上面有一个广告牌，可以推动大语言模型的潜在空间来调用该函数。就这么简单。你注册工具，你描述如何调用它，然后它就可以工作了。

![32]()

让我们开始将它变成一个真正的编码代理的短暂旅程。我们有一个模拟函数和一个模拟工具，但 get_weather(Melbourne, Australia) 目前什么也不做，所以让我们来修复它。

我们需要做的是创建一个工具，然后调整我们的推理循环来检查它是否想要执行该工具（见上面的第 5 行）。如果它想，就执行该工具（见上面的第 6 行），然后将该工具的结果分配回（见上面的第 7 行）循环中进行更多的推理。

这是我们更新后的序列图，当工具被注册后。

![5409]()

由于我们今天不是在制作一个天气应用程序，让我们转而构建一个编码代理的第一个原语。任何编码代理背后的第一个原语都是一个读取工具。所有编码代理都有一个读取工具，可以将文件读入上下文窗口，可以是整个文件，或者在文件较大的情况下，分块读取。

```go
var ReadFileDefinition = ToolDefinition{
	Name:        "read_file",
	Description: "读取给定相对文件路径的内容。当你想查看文件内容时使用此工具。不要对目录名使用此工具。",
	InputSchema: ReadFileInputSchema,
	Function:    ReadFile,
}
```

让我们再次打开我们的工作坊材料（上面）并创建一个文件：

```
cat << EOF
我有鬃毛但不是狮子，
我有四条腿但不是桌子，
我会飞奔但不是跑步，
人们说我不好相处，因为我总是说“嘶”。
我是什么？
EOF > riddle.txt
```
然后让我们运行读取文件工具：

```go
go run read.go
```

你：Claude，伙计。riddle.txt 里的这个谜语的答案是什么？

Claude：我会帮你解开这个谜语。让我先读取 riddle.txt 文件的内容，看看谜语是什么。

工具：read_file({"path":"riddle.txt"})

正在读取文件：riddle.txt
成功读取文件 riddle.txt (175 字节)

结果：我有鬃毛但不是狮子，
我有四条腿但不是桌子，
我会飞奔但不是跑步，
人们说我不好相处，因为我总是说“嘶”。
我是什么？

Claude：读完谜语后，我可以为你解开它：

谜语的答案是：一匹马。

让我们分析一下线索：

“我有鬃毛但不是狮子” - 马有鬃毛，是从脖子顶部生长的毛发。

“我有四条腿但不是桌子” - 马用四条腿走路。

“我会飞奔但不是跑步” - 飞奔是马的一种特定步态。

“人们说我不好相处，因为我总是说‘嘶’” - “嘶”是马发出的声音，听起来像“nay”（意思是“不”或不同意）。

所有这些线索都指向马是这个谜语的答案。

任何编码代理背后的第二个原语是一个列表工具。所有编码代理都有一个列出文件的工具，它列出给定路径中的所有文件和目录。

```go
var ListFilesDefinition = ToolDefinition{
	Name:        "list_files",
	Description: "列出给定路径下的文件和目录。如果未提供路径，则列出当前目录中的文件。",
	InputSchema: ListFilesInputSchema,
	Function:    ListFiles,
}
```

让我们再次打开我们的工作坊材料（上面）并通过以下方式运行基本的聊天应用程序：

```go
go run list_files.go
```

你：Claude，伙计。这个文件夹里有 README.md 吗？另外 riddle.txt 的内容是什么？

Claude：我会帮你检查 README.md 文件并向你展示 riddle.txt 的内容。

首先，让我列出当前目录中的文件，看看 README.md 是否存在：

工具：list_files({})
正在列出目录中的文件：.
成功列出 . 中的 88 个项目

结果：

[".devenv.flake.nix",".direnv/",".envrc",".gitignore","AGENT.md","Makefile","README.md","bash_tool.go","chat.go","code_search_tool.go","devenv.lock","devenv.nix","devenv.yaml","edit_tool.go","go.mod","go.sum","list_files.go","prompts/","prompts/00-weather.md","prompts/01-read_file.md","prompts/02-list_files.md","prompts/03-bash_tool.md","prompts/04-edit_tool.md","read.go","renovate.json","riddle.txt"]

Claude：是的，这个文件夹里有一个 README.md 文件。

现在，让我向你展示 riddle.txt 的内容：

工具：read_file({"path":"riddle.txt"})
结果：我有鬃毛但不是狮子，
我有四条腿但不是桌子，
我会飞奔但不是跑步，
人们说我不好相处，因为我总是说“嘶”。
我是什么？

Claude：riddle.txt 的内容是一个谜语：

我有鬃毛但不是狮子，
我有四条腿但不是桌子，
我会飞奔但不是跑步，
人们说我不好相处，因为我总是说“嘶”。
我是什么？

这个谜语的答案似乎是一匹马。
砰！我们现在有了一个读取文件工具和一个列出文件工具，你可以看到它们的函数从我们最初的提示中被调用和链接在一起。

任何编码工具背后的第三个原语是一个 bash 工具，它的作用正如其名。它使大语言模型能够在你的计算机上执行命令。

```go
var BashDefinition = ToolDefinition{
	Name:        "bash",
	Description: "执行一个 bash 命令并返回其输出。使用此工具来运行 shell 命令。",
	InputSchema: BashInputSchema,
	Function:    Bash,
}
```

让我们再次打开我们的工作坊材料（上面）并运行 bash 工具：

```go
go run bash_tool.go
```

你：嘿 Claude，提供一个在这台电脑上运行的所有进程的摘要。

Claude：我会提供一个在这台电脑上运行的所有进程的摘要。让我使用 ps 命令来收集这些信息。

工具：bash({"command":"ps aux"})
正在执行 bash 命令：ps aux
Bash 命令成功：ps aux (输出：87344 字节)

结果：<已剪切>

Claude：根据 ps aux 命令的输出，以下是这台电脑上运行的进程的摘要：

系统摘要：

这是一个多 CPU 系统，有 96 个核心/线程（从许多与 CPU 相关的内核线程中可以看出）

运行 Linux，使用 systemd 作为初始化系统

系统自 8 月 20 日以来一直处于运行状态（在 START 列中可见）

任何编码工具背后的第四个原语是编辑工具。编辑工具的作用也正如其名。它从推理中获取结果并对文件应用编辑。

GitHub - ghuntley/how-to-build-a-coding-agent
通过在 GitHub 上创建一个帐户，为 ghuntley/how-to-build-a-coding-agent 的开发做出贡献。

GitHub
ghuntley

让我们再次打开我们的工作坊材料（上面）并运行编辑工具：

```go
go run edit_file.go
```

你：Claude，伙计。用 nodejs 创建 fizzbuzz.js，实现 fizzbuzz 到 20，并执行它以确保它能工作。

Claude：我会创建一个 FizzBuzz 的 Node.js 实现，计数到 20，然后运行它以确保它能正常工作。

首先，让我创建 fizzbuzz.js 文件：

```go
工具：edit_file({"path":"fizzbuzz.js","old_str":"","new_str":"// Node.js 中的 FizzBuzz 实现\n// 打印从 1 到 20 的数字\n// 如果数字能被 3 整除，打印 \"Fizz\"\n// 如果数字能被 5 整除，打印 \"Buzz\"\n// 如果数字能被 3 和 5 整除，打印 \"FizzBuzz\"\n\nfor (let i = 1; i <= 20; i++) {\n if (i % 3 === 0 && i % 5 === 0) {\n console.log(\"FizzBuzz\");\n } else if (i % 3 === 0) {\n console.log(\"Fizz\");\n } else if (i % 5 === 0) {\n console.log(\"Buzz\");\n } else {\n console.log(i);\n }\n}"})

正在编辑文件：fizzbuzz.js (用 485 个字符替换 0 个字符)

文件不存在，正在创建新文件：fizzbuzz.js
正在创建新文件：fizzbuzz.js (485 字节)
成功创建文件 fizzbuzz.js

结果：成功创建文件 fizzbuzz.js

Claude：现在让我验证文件是否已创建：
工具：read_file({"path":"fizzbuzz.js"})

正在读取文件：fizzbuzz.js
成功读取文件 fizzbuzz.js (485 字节)

Claude：现在我将执行该脚本以确保它能正常工作：

工具：bash({"command":"node fizzbuzz.js"})
正在执行 bash 命令：node fizzbuzz.js

Bash 命令执行成功，输出长度：77 个字符
结果：

1
2
Fizz
4
Buzz
Fizz
7
8
Fizz
Buzz
11
Fizz
13
14
FizzBuzz
16
17
Fizz
19
Buzz

Claude：太棒了！fizzbuzz.js 文件已成功创建并执行。输出显示：

不能被 3 或 5 整除的数字按原样打印 (1, 2, 4, 7, 8, 11, 13, 14, 16, 17, 19)

能被 3 整除（但不能被 5 整除）的数字打印 "Fizz" (3, 6, 9, 12, 18)

能被 5 整除（但不能被 3 整除）的数字打印 "Buzz" (5, 10, 20)

数字 15 打印 "FizzBuzz"，因为它能被 3 和 5 整除
瞧，我们现在有了一个编码代理。在这一点上，我们可以输入一些其他的提示，然后说，“嘿，让我们做 FizzBuzz 到 100”，它就会工作。


任何编码工具背后的第五个也是最后一个原语是一个搜索工具。如果我告诉你，索引源代码或任何智能都没有什么魔法，你会怎么想？

```go
var CodeSearchDefinition = ToolDefinition{
	Name: "code_search",
	Description: `使用 ripgrep (rg) 搜索代码模式。

使用此工具查找代码模式、函数定义、变量用法或代码库中的任何文本。
你可以按模式、文件类型或目录进行搜索。`,
	InputSchema: CodeSearchInputSchema,
	Function:    CodeSearch,
}
```
让我们再次打开我们的工作坊材料并运行代码搜索工具：

```go
go run code_search_tool.go
```

你：Claude，伙计。在源代码中搜索一个谜语。

Claude：我很乐意在源代码中搜索一个谜语。让我使用代码搜索工具在代码库中查找谜语的提及。

工具：code_search({"pattern":"riddle"})

正在搜索模式：riddle
找到 6 个匹配项：riddle

结果：

./README.md:332:- riddle.txt: 用于内容分析的简单谜语

Claude：我在代码库中找到了对一个名为“riddle.txt”的谜语文件的引用。让我通过列出当前目录中的文件来检查此文件是否存在。

工具：list_files({})
正在列出目录中的文件：.
成功列出 . 中的 25 个文件
结果：

["./go.mod","./renovate.json","./devenv.yaml","./AGENT.md","./.devenv.flake.nix","./.envrc","./bash_tool.go","./fizzbuzz.js","./code_search_tool.go","./list_files.go","./Makefile","./devenv.nix","./devenv.lock","./prompts/01-read_file.md","./prompts/02-list_files.md","./prompts/00-weather.md","./prompts/03-bash_tool.md","./prompts/04-edit_tool.md","./chat.go","./go.sum","./riddle.txt","./edit_tool.go","./README.md","./.gitignore","./read.go"]

Claude：太棒了！我可以看到“riddle.txt”存在于当前目录中。让我读取其内容以查看谜语是什么。

工具：read_file({"path":"riddle.txt"})

正在读取文件：riddle.txt
成功读取文件 riddle.txt (175 字节)
结果：我有鬃毛但不是狮子，
我有四条腿但不是桌子，
我会飞奔但不是跑步，
人们说我不好相处，因为我总是说“嘶”。
我是什么？

Claude：我在 riddle.txt 文件中找到了谜语！

从现在开始的一切都只是调整你的提示的问题。由于时间限制，我们没有深入探讨工具提示的概念。工具提示是你的工具注册的地方。它包含诸如你正在运行的操作系统之类的信息，因此它知道是使用 PowerShell (Windows) 还是 bash (Linux 或 macOS)。

它还包含有关代理应如何操作的说明。我说“应该”是因为大语言模型是非确定性的。你可以包含指导，而它只是指导。但是，通过提示评估、调整和花时间玩模型以了解它们的行为方式，你可以开发出有效的提示。

如果你想看一个成熟的编码代理的例子，请访问下面的博客文章：

总而言之。你刚刚构建的是一个编码代理。也许你不想创建一个编码代理。如果你从事数据工程行业，那会是什么样子？想一想你日常所做的所有活动，在这些活动中，拥有使用这些原语进行自动化的能力对你的雇主来说可能很方便或很有价值。

你现在的同事会抢走你的工作，而不是人工智能。

如果你担心人工智能，答案很简单：只需投资于自己。真的就这么简单。在个人发展方面，今年是一个特别具有挑战性的时期，不容掉以轻心。

